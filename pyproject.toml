[tool.poetry]
name = "u3---insecure-webapi"
version = "0.1.0"
description = ""
authors = ["Alexis"]

[tool.poetry.dependencies]
python = "^3.10"
bottle = "^0.13.4"
mysql-connector-python = "^9.7.0"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
